# newbiethon (택시팟) — 협업 규칙

이 프로젝트는 두 사람이 각자 다른 코딩 에이전트로 작업한다. 이 파일은 어느 도구를 쓰든(Codex, Claude Code 등) 공통으로 읽는 규칙이므로, 도구별 설정 파일(`.claude/`, `CLAUDE.md` 등)의 세부 내용과는 별개로 **여기 적힌 내용은 모든 담당자가 지켜야 한다.**

## 프로젝트 참고 문서

- `PRD.md`: 제품 요구사항 (Why/Who/무엇을/Out of Scope/수용 기준). 구현 전 반드시 먼저 참고한다.
- `PHASE_PLAN.md`: PRD의 Must 요구사항을 구현 순서(Phase 1-A ~ 1-H)로 쪼갠 문서. 프론트/백엔드 작업과 API 엔드포인트가 단계별로 나뉘어 있다. 현재 어느 단계를 진행 중인지는 이 문서 상단에서 갱신한다.

## 기술 스택 (기확정)

- 단일 Next.js 앱 (App Router), Vercel 배포
- 인증: Firebase Authentication (Google 로그인)
- DB: Firebase Firestore
- `frontend/`: 페이지·컴포넌트·클라이언트 상태
- `backend/`: API 라우트, Firebase Admin SDK, Firestore 접근 로직

## 담당 구역

| 디렉터리 | 담당 |
|---|---|
| `frontend/` | 프론트엔드 담당자 (Codex) |
| `backend/` | 백엔드 담당자 (Claude Code) |

각자 자기 디렉터리 밖은 원칙적으로 수정하지 않는다. 상대방 영역에 변경이 필요하다고 판단되면, 직접 고치지 말고 어떤 변경이 왜 필요한지 커밋 메시지나 이슈로 남겨서 상대방이 알게 한다. 담당이 다른 코드가 조용히 섞여 들어가는 것이 이런 분업에서 가장 흔한 문제다.

### 업무량에 따른 확장

지금은 frontend/backend 두 구역뿐이지만, 한쪽 업무량이 뚜렷하게 많아지면 그 구역 안에서 더 세분화한다. 한쪽에만 계속 일이 몰리는 걸 막기 위한 규칙이다.

- 세분화 기준: 한 디렉터리 안에 서로 성격이 다른 영역(예: 마일리지/에스크로, 팟 확정·투표, 정류장 데이터)이 쌓여 업무량이 부담될 정도가 되면, 그 하위 영역을 별도 폴더로 쪼개고(`backend/mileage/`처럼) 그 안에 자체 `AGENTS.md`를 둔다. 상위 `backend/AGENTS.md`·`frontend/AGENTS.md`의 규칙은 하위 폴더에도 그대로 적용되고, 하위 `AGENTS.md`는 그 영역만의 추가 규칙만 적는다.
- 프론트/백엔드 어느 쪽이든 같은 방식으로 세분화한다 — 특정 쪽에만 이 확장 구조를 쓰지 않는다.
- Claude Code 쪽에서 새 하위 영역을 전담하게 되면, `.claude/agents/`에 그 영역 전담 에이전트를 **model: sonnet**으로 추가한다(예: `backend-mileage-dev.md`). 새 에이전트를 추가하면 `CLAUDE.md`의 변경 이력에도 남긴다.

## API 계약

`frontend/`와 `backend/` 사이의 유일한 접점은 API 요청/응답 형태다. 이 형태는 `docs/api-contract.md`에 문서화한다.

- 엔드포인트를 추가하거나 요청/응답 형태를 바꿀 때는, 그 작업을 한 사람이 **그 자리에서** `docs/api-contract.md`도 함께 갱신한다. 나중에 몰아서 정리하면 누락되기 쉽다.
- 기존 계약을 깨는 변경(필드 삭제/추가, 타입 변경, 필수 여부 변경)은 문서에 변경 사유와 날짜를 남긴다.
- 계약 문서와 실제 코드가 다르면, 구현이든 문서든 고친 뒤 커밋 메시지나 문서의 "비고"에 남겨서 상대방이 알아볼 수 있게 한다.
- `PHASE_PLAN.md`에 단계별로 적힌 엔드포인트는 "예정" 목록일 뿐이다. 실제 계약은 구현 시점에 `docs/api-contract.md`에 확정한다.

## 깃 워크플로우

브랜치/PR 없이 **main에 직접 push**한다.

- push 전에 항상 먼저 `git pull`해서 상대방의 최신 변경을 받는다.
- 서로 다른 디렉터리를 담당하므로 코드 충돌은 거의 없지만, `docs/api-contract.md`는 둘 다 건드리는 파일이라 충돌 가능 지점이다 — 충돌 시 두 변경 내용을 모두 남기는 방향으로 병합한다(어느 한쪽을 조용히 지우지 않는다).
- push하기 전, 자신이 API 계약을 건드렸다면 `docs/api-contract.md`가 실제 구현과 맞는지 한 번 더 확인한다.
